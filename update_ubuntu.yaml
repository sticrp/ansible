---
- name: Atualizar servidores Ubuntu
  hosts: all
  become: yes
  become_method: sudo
  gather_facts: yes

  tasks:
    - name: Atualizar lista de pacotes
      apt:
        update_cache: yes

    - name: Atualizar todos os pacotes
      apt:
        upgrade: dist
        autoremove: yes
        autoclean: yes
